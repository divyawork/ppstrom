// This is a simple JavaScript code that displays a message in a web browser

// Define a function that displays the message
function showMessage() {
  // Use the window.alert() method to show a popup message
  window.alert("Hello, World!");
}

// Call the showMessage() function to display the message
showMessage();
